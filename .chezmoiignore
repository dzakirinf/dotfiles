# Matched against target paths relative to $HOME. chezmoi add refuses these too.

# Without this, apply would drop it into $HOME as a target file.
README.md

# Tracking these would pin every machine to the one they were added from.
.config/chezmoi
.gitconfig

.claude/.credentials.json
.claude.json
.ssh
.aws
.azure
.azure-profiles
.config/gh
.docker
.gnupg
.npmrc
.terraform.d
.git-credentials
.netrc
.bash_history

# projects/ is a verbatim archive of past sessions; the rest is regenerable.
.claude/projects
.claude/file-history
.claude/sessions
.claude/session-env
.claude/history.jsonl
.claude/shell-snapshots
.claude/paste-cache
.claude/cache
.claude/backups
.claude/debug
.claude/downloads
.claude/ide
.claude/tasks
.claude/todos
.claude/statsig
.claude/plugins
.claude/stats-cache.json
.claude/mcp-needs-auth-cache.json
.claude/chezmoistate.boltdb
.claude/*.bak

# Rewritten by hunk on update.
.config/hunk/state.json

# Symlinks into ~/.agents/skills; their installer recreates them.
.agents

# Never tracked anywhere: these document internal service names.
.claude/skills/twg*
.claude/skills/waf-false-positive
.claude/skills/resolving-critical-vulns
.claude/skills/concept-docs
